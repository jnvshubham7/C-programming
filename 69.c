#include <stdio.h>
int main()
{
    int a = 5, b = 4;
    int sum, differnce, product;
    sum = a + b;
    printf("sum = a+b = %d\n", sum);
    differnce = a - b;
    printf("difference = a-b = %d\n", differnce); 
    product = a*b;
     printf("product = a*b = %d", product);

    return 0;
}