#include <stdio.h>
int main()
{
    int a = 64, b = 4;
    int quotient, remainder;
    quotient = a/b;
    remainder = a%b;
    printf("quotient = %d\n", quotient);
    printf("remainder = %d", remainder);

   
    return 0;
}